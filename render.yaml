services:
  - type: web
    name: douit
    runtime: python
    region: frankfurt
    plan: free
    buildCommand: bash build.sh
    startCommand: gunicorn config.wsgi:application
    healthCheckPath: /
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: config.settings.production
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: .onrender.com,douit.com.ua,www.douit.com.ua
      - key: CSRF_TRUSTED_ORIGINS
        value: https://*.onrender.com,https://douit.com.ua,https://www.douit.com.ua
      - key: DATABASE_URL
        fromDatabase:
          name: douit-db
          property: connectionString
      - key: PYTHON_VERSION
        value: "3.12.7"
      - key: WEB_CONCURRENCY
        value: "2"

databases:
  - name: douit-db
    region: frankfurt
    plan: free
    databaseName: douit
    user: douit
